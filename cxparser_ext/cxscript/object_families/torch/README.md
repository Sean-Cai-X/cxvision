# Torch object family

These scripts are cxscript semantic smoke cases only.

They must be observed by `cxparser_ext` debug layer as object declarations,
method calls, return objects, and object assignments.

They do not require real torch model loading, inference, training, or GUI
linkage.
