# Mlpack object family

These scripts are cxscript semantic smoke cases only.

The debug layer should expose `model.train(dataset)` and `model.infer(sample)`
as method calls with returned object assignments.

No real mlpack training, inference, or GUI linkage is required.
