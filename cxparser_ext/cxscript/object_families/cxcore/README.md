# Cxcore object family

These scripts are cxscript semantic smoke cases only.

The debug layer should expose `measure.run(input_image)` as a method call and
`result` as the returned object assignment.

No real cxcore measurement backend or GUI linkage is required.
