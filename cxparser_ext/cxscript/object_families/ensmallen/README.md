# Ensmallen object family

These scripts are cxscript semantic smoke cases only.

The debug layer should expose optimize and replay as source object -> method
call -> returned object -> lhs variable chains.

No real optimizer backend or GUI linkage is required.
