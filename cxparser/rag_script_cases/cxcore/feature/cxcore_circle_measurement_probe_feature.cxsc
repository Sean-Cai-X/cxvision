kind=module
layer=feature
module=cxcore
case=circle_measurement_probe
mode=build-run
report=on
input source_image;
input roi_circle;
call measure_circle();
expect success == true;
expect task_id == "cxcore.feature.circle_measurement_probe";
expect scalar_result == 1041.000000;
emit task_id;
emit summary;
