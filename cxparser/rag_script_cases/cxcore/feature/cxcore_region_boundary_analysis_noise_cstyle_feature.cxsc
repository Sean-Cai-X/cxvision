kind=module
layer=feature
module=cxcore
case=region_boundary_analysis_noise
mode=build-run
report=on
use Image;
use Roi;
use ImageAnalysisOutput;
Image source_image;
Roi roi_main;
ImageAnalysisOutput region_result;
analyze_region_boundary(source_image, roi_main);
check(success == true);
check(task_id == "cxcore.feature.region_boundary_analysis_noise");
check(result_object == "ImageAnalysisOutput");
check(runtime_ms >= 0.000000);
check(failure_mode == "handled_noise_condition");
check(summary == "task validated lane=default scalar=10343.000000");
print(task_id);
print(summary);
