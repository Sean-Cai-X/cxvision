global.torch_task = "torch.train.mobilevit.mainline";
global.torch_device = "cpu";
global.torch_input_image = "";
global.torch_dataset_root = "";

global.current_status = "torch_train_start";

if (global.torch_input_image != "") {
    global.current_status = "torch_train_input_available";
}

global.current_status = "torch_train_completed";