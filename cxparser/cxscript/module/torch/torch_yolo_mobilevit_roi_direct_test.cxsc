global.torch_task = "torch.scenario.yolo_mobilevit.infer";
global.torch_device = "auto";
global.torch_input_image = "";
global.torch_dataset_root = "";

global.current_status = "torch_yolo_mobilevit_start";

if (global.torch_input_image != "") {
    global.current_status = "torch_yolo_mobilevit_input_available";
}

global.current_status = "torch_yolo_mobilevit_completed";