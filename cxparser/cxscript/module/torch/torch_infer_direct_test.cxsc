global.torch_task = "torch.infer.mobilevit.unified";
global.torch_device = "auto";
global.torch_input_image = "";
global.torch_dataset_root = "";

global.current_status = "torch_infer_start";

if (global.torch_input_image != "") {
    global.current_status = "torch_infer_input_available";
}

global.current_status = "torch_infer_completed";