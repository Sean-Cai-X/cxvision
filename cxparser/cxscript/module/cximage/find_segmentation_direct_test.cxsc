Image m_image;
FindSegmentation m_seg;

m_image.copyFromMat(global.matInput);

int x0 = global.roi_x0;
int y0 = global.roi_y0;
int x1 = global.roi_x1;
int y1 = global.roi_y1;
int px = global.seed_x;
int py = global.seed_y;
double threshold = 0.5;

m_seg.setbackend("opencv_smoke");
m_seg.setdevice("auto");
m_seg.setthreshold(threshold);
m_seg.setpromptrect(x0, y0, x1, y1);
m_seg.setpoint(px, py);
m_seg.setmode(2);

m_seg.segment(&m_image);
m_seg.extractboundary();
m_seg.buildoverlay(&m_image);

global.current_status = "PENDING";